<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Doggy Dreams Depot</title>
    <!-- Stylesheets -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.dribbble.com/assets/welcome/index-f1c44a1eb3aca69698ea3908d1c6a7820f8e2804808ff0f0d8ad2932f87bbe4a.css">
    <!-- JavaScript -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container">
    <a class="navbar-brand" href="<%= root_path %>">Doggy Dreams Depot</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link" href="<%= about_path %>">About Us</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="<%= contact_path %>">Contact Us</a>
        </li>
      </ul>
      <ul class="navbar-nav">
        <% if user_signed_in? %>
          <li class="nav-item">
            <a href="<%= edit_user_registration_path %>" class="nav-link"><%= current_user.email %></a>
          </li>
          <li class="nav-item">
            <a href="<%= carts_path %>" class="nav-link">My Cart</a>
          </li>
          <li class="nav-item">
            <%= form_for(current_user, url: destroy_user_session_path, method: :delete) do |f| %>
              <%= f.submit "Sign out", class: "nav-link" %>
            <% end %>
          </li>
        <% else %>
          <li class="nav-item">
            <a href="<%= new_user_registration_path %>" class="nav-link">Sign up</a>
          </li>
          <li class="nav-item">
            <a href="<%= new_user_session_path %>" class="nav-link">Sign in</a>
          </li>
          <li class="nav-item">
            <a href="<%= admin_root_path %>" class="nav-link">Admin</a>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>

<main>
  <%= yield %>
</main>

<footer class="bg-light p-3 mt-4">
  <div class="container">
    <p>&copy; 2024 DoggyDreamsDepot. All rights reserved.</p>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a href="<%= admin_root_path %>" class="nav-link">Admin</a>
          </li>
        </ul>
      </div>
    </nav>
  </div>
</footer>

</body>
</html>
